# Tauri icons live here. Replace with real artwork during E9 (hardening / launch).
# Required formats: 32x32.png, 128x128.png, 128x128@2x.png, icon.icns (macOS), icon.ico (Windows).
# Generate via: pnpm tauri icon path/to/source.png
